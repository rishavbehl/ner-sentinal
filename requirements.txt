# NER Logistics Sentinel
#
# The app is built on Starlette (the ASGI core FastAPI itself is built on) so it
# runs with a minimal dependency set. If you prefer FastAPI's decorator style,
# `pip install fastapi` and the same handlers port over directly — nothing in
# backend/ depends on Starlette internals beyond Route/Response.
#
# Everything below except uvicorn's optional extras is already present in most
# scientific Python images.

fastapi>=0.110.0
starlette>=0.37
uvicorn>=0.27
pydantic>=2.0
scikit-learn>=1.3
xgboost>=2.0
pandas>=2.0
numpy>=1.24
networkx>=3.0
joblib>=1.3
python-multipart>=0.0.6   # only needed for photo uploads from the field PWA

# Optional — enables the /ws/fleet WebSocket transport in addition to the
# Server-Sent Events stream the dashboard uses by default:
# websockets>=12.0
